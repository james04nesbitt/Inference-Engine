load("@rules_cc//cc:defs.bzl", "cc_library")

cc_library(
    name = "compute",
    srcs = [
        "simd_kernels.cc",
        "thread_pool.cc",
    ],
    hdrs = [
        "simd_kernels.h",
        "thread_pool.h",
    ],
    visibility = ["//visibility:public"],
    deps = [
        "//engine/tensor",
        "@highway//:hwy",
    ],
)

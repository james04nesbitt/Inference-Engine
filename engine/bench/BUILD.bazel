load("@rules_cc//cc:defs.bzl", "cc_binary")

cc_binary(
    name = "matmul_bench",
    srcs = ["matmul_bench.cc"],
    defines = ["HWY_STATIC_DEFINE"],
    deps = [
        "//engine/compute",
    ],
)
